#include "PhoneBook.hpp"

int main(void)
{
    std::string input;
    while (true)
    {
        std::getline(std::cin, input);
        if (input == "ADD")
        else if (input == "SEARCH")
        else if (input == "EXIT")
    }
}