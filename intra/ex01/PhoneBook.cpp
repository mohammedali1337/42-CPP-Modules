#include "PhoneBook.hpp"

int main(void)
{
    std::string input;
    while (true)
    {
        if (!std::getline(std::cin, input))
            return 1;
        // if (input == "ADD")

        // else if (input == "SEARCH")
        // else if (input == "EXIT")
    }
}